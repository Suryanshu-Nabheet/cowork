{
  "name": "cowork",
  "private": true,
  "version": "1.0.0",
  "license": "MIT",
  "author": "Suryanshu Nabheet <suryanshunab@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/Suryanshu-Nabheet/cowork.git"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo dev --filter=@cowork/api --filter=@cowork/worker --filter=@cowork/web --filter=@cowork/sandbox-supervisor",
    "build": "turbo build",
    "check": "turbo check",
    "lint": "biome check .",
    "format": "biome check --write .",
    "db:generate": "pnpm --filter @cowork/db generate",
    "db:migrate": "pnpm --filter @cowork/db migrate",
    "sandbox:build": "docker build -t cowork/computer:local infra/sandboxes/computer",
    "verify:fast": "vitest run",
    "verify": "tsx packages/testkit/src/cli/verify.ts",
    "verify:providers": "tsx packages/testkit/src/cli/verify.ts --providers",
    "e2e": "pnpm --filter @cowork/web exec playwright test",
    "compose:up": "docker compose -f infra/compose/docker-compose.yml up --build",
    "compose:down": "docker compose -f infra/compose/docker-compose.yml down -v"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "@types/node": "^24.3.0",
    "tsx": "^4.20.5",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2",
    "vitest": "^4.1.10"
  },
  "pnpm": {
    "overrides": {
      "react": "19.0.0",
      "react-dom": "19.0.0"
    }
  }
}
